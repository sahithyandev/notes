---
title: Norm
sidebar:
  order: 6
slug: s4/linear-algebra/norm
prev: true
next: true
dateCreated: 2026-04-23T17:39:39.626Z
lastUpdatedOn: 2026-04-24T03:46:02.230Z
---

Aka. induced norm.

```math
||x||=\sqrt{\langle x,x\rangle}
```

A norm is usually associated with an inner product of a vector space. However any arbitrary operation with the below properties can also be defined as a norm:
- $||x||: V \rightarrow F$ is a function
- $||x|| \ge 0$ and $||x|| = 0 \iff x = \underline{0}$ 
- $||ax|| = |a|\cdot||x||$
- $||x+y|| \le ||x|| + ||y||$

<Note>

If there exists a multiplication operator, $||x\cdot y || \le ||x|| \cdot ||y||$

</Note>

## Properties

### Multiplication

```math
||ax|| = |a| \cdot ||x||
```

### Expansion

```math
|| ax + by || = |a|^2 ||x||^2 + 2\text{Re}(\overline{a}b\langle x,y\rangle) + |b|^2 ||y||^2
```

### Triangle Inequality

```math
|| x + y || \le ||x|| + ||y||
```

### Cauchy-Schwarz Inequality

```math
||\langle x,y\rangle|| \le ||x|| \cdot ||y||
```

<Note title="Proof Hint">

Start with:

```math
\bigg\lvert \bigg\lvert x - \frac{\overline{\langle x,y \rangle}}{||y||^2}y \bigg\rvert \bigg\rvert
```

</Note>

## Normed Space

A vector space with a norm.

### Distance

For 2 vectors, $x,y$, distance between the 2 vectors is given by $||x-y||$.
