---
title: Non-deterministic Finite Automaton
sidebar:
  order: 8
  label: NFA
slug: s4/theory-of-computing/non-deterministic-finite-automaton
prev: true
next: true
dateCreated: 2026-04-23T17:39:39.632Z
lastUpdatedOn: 2026-06-11T13:19:10.649Z
---

Aka. NFA. Multiple transitions for the same input symbol are allowed. Can have multiple possible starting states.

More flexible in terms of construction and may simplify the process of creating an FA for a regular language. Useful for theorem-proving.

## Transition Function

Defined as $\delta: Q \times \Sigma \rightarrow 2^Q$. The function maps a state and an input symbol to a set of possible next states, rather than a single state as in DFA.

<Note title="Power Set">

Power set of a set $S$ is the set of all subsets of $S$. Denoted by $2^S$. If $S$ has $n$ elements, then $2^S$ has $2^n$ elements.

</Note>

### Extended Transition Function

Defined as $\delta^*: Q \times \Sigma^* \rightarrow 2^Q$. It handles strings instead of individual symbols. $\delta^*(q, x)$ is the set of states reachable from state $q$ after reading string $x$.

Rules:

- $\delta^*(q, \lambda) = \{q\}$
- $\delta^*(q, ya) = \bigcup_{p \in \delta^*(q, y)} \delta(p, a)$  
  Union of the sets of states reachable from each state in $\delta^*(q, y)$ after reading symbol $a$.

## Acceptance

Let $M = (Q, \Sigma, q_0, A, \delta)$.

A string $x \in \Sigma^*$ is accepted **iff** $\delta^*(q_0, x) \cap A \neq \varnothing$.

A string is accepted if at least one of the states reachable from the start state after reading the string is an accepting state.

## NFA Complement

Convert to DFA first, then [complement](/s4/theory-of-computing/deterministic-finite-automaton#complement).
