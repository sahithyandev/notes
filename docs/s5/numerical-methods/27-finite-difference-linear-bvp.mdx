---
title: Linear Finite-Difference Method
dateCreated: 2026-09-23T08:09:15.129Z
lastUpdatedOn: 2026-09-23T08:17:14.029Z
slug: s5/numerical-methods/finite-difference-linear-bvp
sidebar:
  order: 27
prev: true
next: true
---

The finite-difference method approximates the solution of a linear [BVP](/s5/numerical-methods/boundary-value-problems) by replacing derivatives with centered difference formulas at mesh points and solving the resulting linear system.

## Standard Form

$$
y'' = p(x)y' + q(x)y + r(x), \quad a \leq x \leq b, \quad y(a) = \alpha,\ y(b) = \beta
$$

## Mesh Points

Partition $[a,b]$ into $N+1$ subintervals of length $h = \dfrac{b-a}{N+1}$. Mesh points are

$$
x_i = a + ih, \quad i = 0, 1, \ldots, N+1
$$

## Centered Difference Formulas

$$
y''(x_i) = \frac{1}{h^2}\big(y(x_{i+1}) - 2y(x_i) + y(x_{i-1})\big) - \frac{h^2}{12}y^{(4)}(\xi_i)
$$

$$
y'(x_i) = \frac{1}{2h}\big(y(x_{i+1}) - y(x_{i-1})\big) - \frac{h^2}{6}y'''(\eta_i)
$$

for some $\xi_i, \eta_i \in (x_{i-1}, x_{i+1})$.

## Difference Equation

Substituting the centered difference formulas into the BVP and dropping the $O(h^2)$ error term, $y(x_i) \approx w_i$ satisfies

$$
w_0 = \alpha, \quad w_{N+1} = \beta
$$

$$
-\left(1 + \frac{h}{2}p(x_i)\right)w_{i-1} + \big(2 + h^2 q(x_i)\big)w_i - \left(1 - \frac{h}{2}p(x_i)\right)w_{i+1} = -h^2 r(x_i)
$$

for $i = 1, \ldots, N$, with truncation error $O(h^2)$.

## Matrix Form

$$
Aw = b
$$

with $A$ tridiagonal

$$
A = \begin{pmatrix}
2+h^2q(x_1) & -1+\frac{h}{2}p(x_1) & & \\
-1-\frac{h}{2}p(x_2) & 2+h^2q(x_2) & -1+\frac{h}{2}p(x_2) & \\
& \ddots & \ddots & \ddots \\
& & -1-\frac{h}{2}p(x_N) & 2+h^2q(x_N)
\end{pmatrix}
$$

$$
w = \begin{pmatrix} w_1 \\ w_2 \\ \vdots \\ w_{N-1} \\ w_N \end{pmatrix}, \quad
b = \begin{pmatrix}
-h^2r(x_1) + \left(1+\frac{h}{2}p(x_1)\right)\alpha \\
-h^2r(x_2) \\
\vdots \\
-h^2r(x_{N-1}) \\
-h^2r(x_N) + \left(1-\frac{h}{2}p(x_N)\right)\beta
\end{pmatrix}
$$

Solve for $w$ with [LU decomposition](/s5/numerical-methods/lu-decomposition) or another linear solver.

## Existence and Uniqueness

If $p$, $q$, $r$ are continuous on $[a,b]$ and $q(x) \geq 0$ on $[a,b]$, the linear system has a unique solution whenever $h < \dfrac{2}{L}$, where $L = \max_{a \leq x \leq b} |p(x)|$.
