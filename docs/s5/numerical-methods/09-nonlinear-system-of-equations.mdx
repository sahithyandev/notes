---
title: Nonlinear System of Equations
dateCreated: 2026-07-28T05:12:36.350Z
lastUpdatedOn: 2026-07-28T14:02:20.882Z
slug: s5/numerical-methods/nonlinear-system-of-equations
sidebar:
  order: 9
prev: true
next: true
---

A nonlinear system of $n$ equations in $n$ unknowns $x_1, x_2, \ldots, x_n$ is a set of equations of the form

$$
\begin{aligned}
f_1(x_1, x_2, \ldots, x_n) &= 0 \\
f_2(x_1, x_2, \ldots, x_n) &= 0 \\
&\;\vdots \\
f_n(x_1, x_2, \ldots, x_n) &= 0
\end{aligned}
$$

where each $f_i$ maps a vector $\vec{x} = (x_1, x_2, \ldots, x_n)^T \in \mathbb{R}^n$ into $\mathbb{R}$.

In vector form, $F(\vec{x}) = \vec{0}$, where

$$
F(\vec{x}) = \big(f_1(\vec{x}), f_2(\vec{x}), \ldots, f_n(\vec{x})\big)^T
$$

Here:

- $F$: maps $\mathbb{R}^n$ into $\mathbb{R}^n$
- $f_1, f_2, \ldots, f_n$: coordinate functions of $F$

## Example

Place the system

$$
\begin{aligned}
3x_1 - \cos(x_2 x_3) - \tfrac{1}{2} &= 0 \\
x_1^2 - 81(x_2 + 0.1)^2 + \sin x_3 + 1.06 &= 0 \\
e^{-x_1 x_2} + 20x_3 + \tfrac{10\pi - 3}{3} &= 0
\end{aligned}
$$

in the form $F(\vec{x}) = \vec{0}$.

The coordinate functions are

$$
\begin{aligned}
f_1(x_1, x_2, x_3) &= 3x_1 - \cos(x_2 x_3) - \tfrac{1}{2} \\
f_2(x_1, x_2, x_3) &= x_1^2 - 81(x_2 + 0.1)^2 + \sin x_3 + 1.06 \\
f_3(x_1, x_2, x_3) &= e^{-x_1 x_2} + 20x_3 + \tfrac{10\pi - 3}{3}
\end{aligned}
$$

so $F(\vec{x}) = \big(f_1(\vec{x}), f_2(\vec{x}), f_3(\vec{x})\big)^T$.

## Methods

Numerical methods for solving nonlinear systems generalize the single-variable case.

### Fixed-Point Iteration

Rewrites $F(\vec{x}) = \vec{0}$ as $\vec{x} = G(\vec{x})$, then iterates $\vec{x}^{(k)} = G(\vec{x}^{(k-1)})$. See [fixed point](/s5/numerical-methods/fixed-point).

### Newton's Method

Uses the [Jacobian matrix](/s5/numerical-methods/jacobian-matrix) of $F$ to get quadratic convergence to a root of $F(\vec{x}) = \vec{0}$. See [Newton's method](/s5/numerical-methods/newtons-method).
