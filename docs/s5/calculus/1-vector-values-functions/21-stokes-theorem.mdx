---
title: Stokes' Theorem
sidebar:
  order: 21
dateCreated: 2026-08-14T00:00:00.000Z
lastUpdatedOn: 2026-09-25T06:53:40.471Z
slug: s5/calculus/vector-values-functions/stokes-theorem
prev: true
next: true
---

### Positive Orientation of a Boundary Curve

Let $\sigma$ be an [oriented surface](/s5/calculus/vector-values-functions/orientation) with unit normal field $\hat{n}$, and let $C$ be the boundary curve of $\sigma$. The positive orientation of $C$ is the direction determined by the chosen orientation of $\sigma$.

Equivalently, walking along $C$ with the head pointing in the direction of $\hat{n}$ keeps the surface on the left.

## Statement

Let $\sigma$ be a piecewise smooth oriented surface bounded by a positively oriented, piecewise smooth, simple closed curve $C$. Suppose

$$
\boldsymbol{F}(x,y,z) = f(x,y,z)\hat{i} + g(x,y,z)\hat{j} + h(x,y,z)\hat{k}
$$

is a [vector field](/s5/calculus/vector-values-functions/vector-field) whose component functions have continuous first-order partial derivatives on an open set containing $\sigma$. If $\hat{n}$ is the chosen unit normal field for $\sigma$,

$$
\oint_C \boldsymbol{F} \cdot \text{d}\boldsymbol{r} = \iint_\sigma (\text{curl}\,\boldsymbol{F}) \cdot \hat{n}\,\text{d}S
$$

<Note>

Green's Theorem is the special case $\sigma = D$, $\hat{n} = \hat{k}$, $\boldsymbol{F}(x,y,z) = M(x,y)\hat{i} + N(x,y)\hat{j}$, where $D$ is a region in the $xy$-plane. Then

$$
\text{curl}\,\boldsymbol{F} = \left(\frac{\partial N}{\partial x} - \frac{\partial M}{\partial y}\right)\hat{k}
$$

and Stokes' Theorem gives

$$
\oint_C M\,\text{d}x + N\,\text{d}y = \iint_D \left(\frac{\partial N}{\partial x} - \frac{\partial M}{\partial y}\right)\text{d}A
$$

</Note>
