---
title: Introduction to Complex Functions
dateCreated: 2026-07-01T06:36:46.266Z
lastUpdatedOn: 2026-09-04T02:55:52.932Z
slug: s5/calculus/complex-functions/introduction
sidebar:
  label: Introduction
  order: 1
prev: false
next: true
---

Let $S$ be a set of complex numbers. A function $f: S \to \mathbb{C}$ is a rule that assigns to each $z \in S$ a complex number $\omega$, written $\omega = f(z)$. $S$ is the domain of $f$.

## Real and Imaginary Parts

If $z = x+iy$, $f(z)$ decomposes into a pair of real-valued functions of $x$ and $y$:

$$
f(z) = u(x,y) + iv(x,y)
$$

$u$ is the real part of $f$, $v$ is the imaginary part of $f$.

If $z = re^{i\theta}$,

$$
f(z) = f(re^{i\theta}) = u(r,\theta) + iv(r,\theta)
$$

## Mapping

A function $f$ is also called a mapping or transformation. The image of a point $z \in S$ is $\omega = f(z)$.

- If $T \subseteq S$, the image of $T$ is the set of images of all points in $T$.
- The image of the entire domain $S$ is the range of $f$.
- The inverse image of a point $\omega$ is the set of all $z$ in the domain of $f$ with $f(z) = \omega$.

<Note>

Translation, rotation, and reflection describe the dominant geometric characteristics of mappings. The $z$-plane and $\omega$-plane are treated as the same plane in these cases.

</Note>

Examples:

- $\omega = z+1$ translates each point $z$ one unit to the right.
- $\omega = e^{i\theta}$ rotates each point $z$ through an angle $\theta$ about the origin, counterclockwise, since $e^{i\theta} = \cos\theta + i\sin\theta$.
- $\omega = iz$ rotates each non-zero point $z$ through a 90 degree angle about the origin, counterclockwise, since $i = e^{i\pi/2}$.
- $\omega = \bar{z}$ reflects each point $z$ about the real axis.
- $\omega = -z$ reflects each point $z$ about the imaginary axis.
