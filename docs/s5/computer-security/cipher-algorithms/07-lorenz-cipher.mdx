---
title: Lorenz Cipher
sidebar:
  order: 7
dateCreated: 2026-07-29T05:56:03.713Z
lastUpdatedOn: 2026-08-04T03:56:01.937Z
slug: s5/computer-security/cipher-algorithms/lorenz-cipher
prev: true
next: true
---

A stream cipher, manufactured by C. Lorenz AG, that encrypts a Baudot-coded plaintext bit stream into 5 ciphertext bit streams.

<Note>

Baudot code, invented by Émile Baudot, encodes each character as 5 bits. A 5-bit code only spans 32 symbols, so 2 state symbols (Figure, Letters) switch between 2 32-symbol alphabets to cover the full alphanumeric set.

</Note>

Encryption XORs the 5-bit Baudot code with a 5-bit random sequence, generated by 12 shift registers in 3 groups:

- 5 $\chi$ registers, lengths 41, 31, 29, 26, 23.
- 5 $\psi$ registers, lengths 43, 47, 51, 53, 59.
- 2 $\mu$ registers, lengths 61, 37.

At each clock tick $t$:

- $K = (\chi_i \oplus \psi_i)$ for $i = 1, \dots, 5$.
- $t \leftarrow t + 1$, and the $\chi$ registers advance.
- $\mu^{(1)}$ advances every tick, and conditionally triggers $\mu^{(2)}$ to advance.
- $\mu^{(2)}$ conditionally triggers the $\psi$ registers to advance.
- Output $K$.

Key space:

$$
2^{41+31+29+26+23+43+47+51+53+59+61+37} = 2^{501}
$$

<Note>

Register settings can be rotated on different schedules to behave like session keys, e.g. $\mu$ registers changed daily, $\chi$ registers monthly, $\psi$ registers quarterly.

</Note>
