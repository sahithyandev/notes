---
title: Basic Solutions
slug: s5/operational-research/basic-solutions
sidebar:
  order: 6
prev: true
next: true
dateCreated: 2026-08-26T11:52:59.925Z
lastUpdatedOn: 2026-08-26T18:01:43.921Z
---

Suppose a linear program in standard form has $m$ constraints and $n$ variables ($n \ge m$).

## Basic Variable

A variable solved for from the constraints rather than set to 0.

## Non-basic Variable

A variable fixed at 0 while solving the constraints for the basic variables.

## Basic Solution

Set $n - m$ variables (non-basic) to 0, and solve the $m$ constraint equations for the remaining $m$ variables (basic). At most $\binom{n}{m}$ basic solutions exist for a given LP.

## Basic Feasible Solution

A basic solution where every variable is also $\ge 0$. Corresponds to an extreme point of the feasible region.

## Degenerate Basic Solution

A basic solution where at least one basic variable equals 0.

## Non-degenerate Basic Solution

A basic solution where every basic variable is strictly positive.
