---
title: Assignment Problem
slug: s5/operational-research/assignment-problem
sidebar:
  order: 15
prev: true
next: true
dateCreated: 2026-08-26T11:55:10.616Z
lastUpdatedOn: 2026-09-23T07:53:12.405Z
---

The problem of assigning $n$ agents to $n$ tasks one-to-one, minimizing total cost, given a cost $c_{ij}$ for assigning agent $i$ to task $j$. A special case of the [transportation problem](/s5/operational-research/transportation-problem).

$$
\text{minimize } z = \sum_{i=1}^{n} \sum_{j=1}^{n} c_{ij} x_{ij}
$$

Subject to $\sum_j x_{ij} = 1$ for every agent, $\sum_i x_{ij} = 1$ for every task, and $x_{ij} \in \{0, 1\}$.

Every supply and demand equals 1, so every basic feasible solution is inherently degenerate ($n$ occupied cells needed for a non-degenerate BFS, but only $n$ assignments exist against $2n - 1$ required).

### Balanced

An assignment problem is balanced **iff** the number of agents equals the number of tasks.

### Unbalanced

When the problem is not balanced. Dummy agents or tasks are addded with 0 cost to rebalance.

### Maximization

A maximization assignment problem is one where the objective is to maximize total cost.

The problem is converted to minimization by subtracting every cost from the largest cost in the matrix, then solve as usual.

### Restricted

An assignment problem is restricted **iff** there are prohibited agent-task pairs.

A prohibited agent-task pair is assigned a very large cost $M$, driving it out of the optimal assignment.

## Hungarian Method

Solves a balanced assignment problem in polynomial time, avoiding the degeneracy the [transportation methods](/s5/operational-research/transportation-problem) would hit.

### Steps

- Row reduction  
  Subtract the minimum entry of each row from every entry in that row.
- Column reduction  
  Subtract the minimum entry of each column from every entry in that column.
- Cover all 0s in the resulting matrix using the minimum number of horizontal and vertical lines.
- If the number of lines equals $n$, an optimal assignment exists among the 0s. Stop.
- Otherwise, find the smallest uncovered entry. Subtract it from every uncovered entry, and add it to every entry covered twice (at a line intersection). Repeat the covering step.

### Reading the Assignment

Once $n$ lines are needed, select $n$ independent 0s (1 per row and column) by assigning rows/columns with a unique 0 first, then resolving remaining choices, until every agent is assigned a task.

## Worked Example

3 agents and 3 tasks, with cost matrix:

<Walkthrough variant="assignment">
  <AssignmentStep
    label="Initial"
    agents={["Agent 1", "Agent 2", "Agent 3"]}
    tasks={["Task 1", "Task 2", "Task 3"]}
    costs={[
      [9, 2, 7],
      [6, 4, 3],
      [5, 8, 1],
    ]}
  >
    Balanced, since 3 agents equal 3 tasks.
  </AssignmentStep>

<AssignmentStep
  label="Row Reduction"
  agents={["Agent 1", "Agent 2", "Agent 3"]}
  tasks={["Task 1", "Task 2", "Task 3"]}
  costs={[
    [7, 0, 5],
    [3, 1, 0],
    [4, 7, 0],
  ]}
>
  Subtract each row's minimum ($2$, $3$, $1$) from every entry in that row.
</AssignmentStep>

<AssignmentStep
  label="Column Reduction"
  agents={["Agent 1", "Agent 2", "Agent 3"]}
  tasks={["Task 1", "Task 2", "Task 3"]}
  costs={[
    [4, 0, 5],
    [0, 1, 0],
    [1, 7, 0],
  ]}
  coveredRows={[1]}
  coveredCols={[1, 2]}
>
  Subtract each column's minimum ($3$, $0$, $0$) from every entry in that
  column. Covering all 0s needs 3 lines (row Agent 2, column Task 2, column Task
  3), equal to $n = 3$: an optimal assignment exists among the 0s.
</AssignmentStep>

  <AssignmentStep
    label="Assignment"
    agents={["Agent 1", "Agent 2", "Agent 3"]}
    tasks={["Task 1", "Task 2", "Task 3"]}
    costs={[
      [9, 2, 7],
      [6, 4, 3],
      [5, 8, 1],
    ]}
    assigned={[
      [0, 1],
      [1, 0],
      [2, 2],
    ]}
    total="z = 2 + 6 + 1 = 9"
  >
    Independent 0s, 1 per row and column: Agent 1 has a single 0, at Task 2.
    Agent 3 has a single 0, at Task 3. Task 1 is then Agent 2's only remaining
    column.
  </AssignmentStep>
</Walkthrough>
