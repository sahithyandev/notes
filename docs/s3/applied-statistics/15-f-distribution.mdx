---
title: F Distribution
sidebar:
  order: 15
slug: s3/applied-statistics/f-distribution
prev: true
next: true
dateCreated: 2026-04-22T07:09:10.681Z
lastUpdatedOn: 2026-09-13T04:35:59.435Z
---

Suppose 2 independent populations (of sizes $N_1$ and $N_2$) are given. And samples of sizes $n_1$ and $n_2$ are taken from each population.

F statistic can be computed using:

$$
F = \cfrac{ \chi_1^2 / \nu_1 }{ \chi_2^2 / \nu_2 }  = \frac{ s_1^2 }{ s_2^2 }\cdot \frac{\sigma_1 ^2}{\sigma_2 ^2}
$$

## Properties

### Degrees of Freedom

Degrees of freedom are $\nu_1 = n_1 - 1$ and $\nu_2 = n_2 - 1$.

### F Reciprocal

$$
F_{\nu_1, \nu_2, \alpha} = \frac{1}{F_{\nu_2, \nu_1, 1 - \alpha}}
$$

## Uses

- Variance comparison  
  Test whether 2 populations have equal variance.
- ANOVA  
  Test whether means of 3 or more groups are equal (ratio of between-group to within-group variance).
- Regression F-test  
  Test whether the regression model as a whole explains a significant portion of variance in the response.

## Hypothesis Test for Equal Variance

$H_0$: $\sigma_1^2 = \sigma_2^2$

Test statistic:

$$
F = \frac{s_1^2}{s_2^2}
$$

Under $H_0$, $F \sim F(\nu_1, \nu_2)$.

Reject $H_0$ if $F > F_{\nu_1, \nu_2, \alpha}$ (right-tail) or compare against both tails for a 2-tailed test. Critical values are read from the F-table for the given $\nu_1$, $\nu_2$, and significance level $\alpha$.
