---
type CalloutType = "note" | "caution" | "danger" | "tip";

interface Props {
  title?: string;
  type?: CalloutType;
}

const { title, type = "note" } = Astro.props;
---

<aside class="callout" data-type={type}>
  {title && <strong class="callout-title">{title}</strong>}
  <slot />
</aside>

<style is:global>
  .callout {
    --cal-accent: var(--sem);
    --cal-bg: var(--sem-bg);
    --cal-border: var(--sem-border);
    --cal-text: var(--sem-text);

    border-radius: 0 8px 8px 0;
    border: 1px solid var(--cal-border);
    border-left: 3px solid var(--cal-accent);
    background: var(--cal-bg);
    padding: 14px 18px;
    margin: 20px 0;
    font-size: 0.9375rem;
    color: var(--cal-text);
    line-height: 1.6;
  }

  .callout strong.callout-title {
    font-family: var(--font-ui);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.4px;
    text-transform: uppercase;
    color: var(--cal-accent);
    margin-bottom: 5px;
    display: block;
  }

  .callout p {
    font-family: var(--font-body);
    color: var(--cal-text);
  }

  .callout p:last-child {
    margin-bottom: 0;
  }

  .callout ul li::before {
    background: var(--cal-accent);
  }

  .callout[data-type="note"] {
    --cal-accent: #2563eb;
    --cal-bg: #eff4ff;
    --cal-border: #c4d5ff;
    --cal-text: #1e3a8a;
  }

  .callout[data-type="tip"] {
    --cal-accent: #16a34a;
    --cal-bg: #ecfdf3;
    --cal-border: #b8e6c9;
    --cal-text: #15803d;
  }

  .callout[data-type="caution"] {
    --cal-accent: #d97706;
    --cal-bg: #fef6e7;
    --cal-border: #f5d79e;
    --cal-text: #92400e;
  }

  .callout[data-type="danger"] {
    --cal-accent: #dc2626;
    --cal-bg: #fef2f2;
    --cal-border: #fcc5c5;
    --cal-text: #991b1b;
  }

  [data-theme="dark"] {
    .callout {
      --cal-bg: color-mix(in oklab, var(--cal-accent) 10%, var(--surface));
      --cal-border: color-mix(in oklab, var(--cal-accent) 24%, var(--surface));
      --cal-text: color-mix(
        in oklab,
        var(--cal-accent) 20%,
        var(--text-primary)
      );
    }

    .callout[data-type="note"] {
      --cal-accent: #60a5fa;
    }

    .callout[data-type="tip"] {
      --cal-accent: #4ade80;
    }

    .callout[data-type="caution"] {
      --cal-accent: #fbbf24;
    }

    .callout[data-type="danger"] {
      --cal-accent: #f87171;
    }
  }
</style>
